import React, { FC } from 'react';
import styles from "./MovieRoomsBlock.module.scss";

const MovieRoomsBlock: FC<{ movieId: number }> = ({ movieId }) => {
    return (
        <div>

        </div>
    );
};

export default MovieRoomsBlock;
